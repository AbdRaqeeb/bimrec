import { firebaseConstant } from '../constants';

export const config = {
  type: firebaseConstant.type,
  project_id: firebaseConstant.project_id,
  private_key_id: firebaseConstant.private_key_id,
  private_key: firebaseConstant.private_key,
  client_email: firebaseConstant.client_email,
  client_id: firebaseConstant.client_id,
  auth_uri: firebaseConstant.auth_uri,
  token_uri: firebaseConstant.token_uri,
  auth_provider_x509_cert_url: firebaseConstant.auth_provider_x509_cert_url,
  client_x509_cert_url: firebaseConstant.client_x509_cert_url,
};
